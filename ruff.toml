line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = [
  "E501",   # line length is handled by the formatter, not worth failing CI over
  "B905",   # zip(strict=) needs 3.10+, and we support 3.10
]

[lint.per-file-ignores]
"tests/*" = ["E402"]
